# Example production env file for TradingXbert
# Copy to `.env.production` and replace values before deploying.

# Your production domain (used to build absolute URLs for metadata)
NEXT_PUBLIC_METADATA_BASE=https://www.tradingxbert.com

# OpenAI API key (server-only, do NOT expose this in client-side code)
OPENAI_API_KEY=sk-REPLACE_WITH_YOUR_KEY

# Optional: Set this to 1 to force the app to use mock responses (dev only)
NEXT_PUBLIC_USE_MOCK=0
